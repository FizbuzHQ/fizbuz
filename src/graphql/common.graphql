fragment FullProfile on User {
    id
    profile {
        id
        nickname
        name
        skills {
            tool {
                name
            }
        }
    }
}

query ToolList {
    tools {
        id
        kind
        name
    }
}